import { ShowScrollButton } from './show-scroll-button';

describe('ShowScrollButton', () => {
  it('should create an instance', () => {
    const directive = new ShowScrollButton();
    expect(directive).toBeTruthy();
  });
});
